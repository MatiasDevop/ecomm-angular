export interface CartProduct {
  addedDate: string;
  cartId: number;
  categoryName: string;
  custId: number;
  productId: number;
  productImageUrl: string;
  productName: string;
  productPrice: number;
  quantity: number;
}
